<form class="space-y-8 divide-y divide-gray-200" method="POST" {{#if post}} action="/dashboard/posts/{{ post.id }}?_method=PUT" {{else}} action="/dashboard/posts" {{/if}}>
  <div class="space-y-8 divide-y divide-gray-200">
    <div>
      <input type="hidden" name="_csrf" value="{{ csrfToken }}">

      <div>
        <h3 class="text-lg leading-6 font-medium text-gray-900">
          {{ title }}
        </h3>
        <p class="mt-1 text-sm text-gray-500">
          {{ description }}
        </p>
      </div>

      <div>
        <div class="sm:col-span-6">
          <label for="content" class="block text-sm font-medium text-gray-700">
            Content
          </label>
          <div class="mt-1">
            <textarea id="content" name="content" rows="3" class="shadow-sm focus:ring-blue-500 focus:border-blue-500 block w-full sm:text-sm border border-gray-300 rounded-md"></textarea>
          </div>
          <p class="mt-2 text-sm text-gray-500">Your entire post.</p>
          {{#if errors.content}}
          <p class="text-sm text-red-500">{{ errors.content }}</p>
          {{/if}}
        </div>
      </div>
    </div>
  </div>

  <div class="pt-5">
    <div class="flex justify-end">
      <a href="/dashboard/posts" class="bg-white py-2 px-4 border border-gray-300 rounded-md shadow-sm text-sm font-medium text-gray-700 hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">
        Cancel
      </a>
      <button type="submit" class="ml-3 inline-flex justify-center py-2 px-4 border border-transparent shadow-sm text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500">
        Save
      </button>
    </div>
  </div>
</form>
